version: 0.2

phases:
  install:
    commands:
      - npm install -g yarn
      - yarn install
  build:
    commands:
      - yarn build
artifacts:
  files:
    - '**/*'
  base-directory: dist
  discard-paths: yes